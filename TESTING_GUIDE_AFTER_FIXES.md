# 🧪 คู่มือทดสอบหลังจากแก้ไข Backend และ Frontend

## ✅ สิ่งที่แก้ไขแล้ว

### Frontend
1. ✅ แก้ไข `RegisterProjectModal.tsx` - เพิ่ม null/undefined checks
2. ✅ Rebuild frontend สำเร็จ

### Backend
1. ✅ แก้ไข `projects/serializers.py`:
   - เพิ่ม import `ProjectStudent`
   - แก้ไข `ProjectLogEntrySerializer` ให้ใช้ field `content` แทน `message`
   - แก้ไข `ProjectSerializer.create()` ให้ handle `academic_year` ถูกต้อง

2. ✅ แก้ไข `projects/views.py`:
   - เพิ่ม error handling ใน `get_queryset()` สำหรับกรณีที่ user ไม่มี profile
   - แก้ไขการ filter projects สำหรับ students
   - แก้ไข `perform_create()` ให้ใช้ `current_academic_year`

## 🚀 ขั้นตอนการทดสอบ

### 1. เริ่ม Backend Server

```powershell
# เปิด PowerShell
cd C:\Users\f15fo\web101
.\venv\Scripts\Activate.ps1
cd web101\backend
python manage.py runserver
```

**ตรวจสอบ:** ควรเห็นข้อความ `Starting development server at http://127.0.0.1:8000/`

### 2. เริ่ม Frontend Server (Terminal ใหม่)

```powershell
# เปิด PowerShell Terminal ใหม่
cd C:\Users\f15fo\web101\web101\frontend
npm run dev
```

**ตรวจสอบ:** ควรเห็นข้อความ `Local: http://localhost:5173/`

### 3. ทดสอบ Register Project

1. เปิด browser ไปที่ `http://localhost:5173`
2. Login ด้วย Student account (เช่น `155n1006_21` / `password123`)
3. คลิกปุ่ม "Register Your Project"
4. กรอกข้อมูล:
   - Topic (Lao)
   - Topic (English)
   - เลือก Advisor
   - เพิ่ม Students (ถ้ามี)
5. คลิก "Submit" หรือ "Register"
6. **ตรวจสอบ:**
   - ไม่มี error ใน console
   - Modal ปิดได้
   - Project ปรากฏในรายการ

### 4. ทดสอบ Milestone Submission

1. เปิด Project ที่สร้างไว้
2. ไปที่แท็บ "Milestones"
3. คลิก "Submit" หรือ "Upload" สำหรับ milestone
4. เลือกไฟล์ที่ต้องการ upload
5. กรอกข้อมูลเพิ่มเติม (ถ้ามี)
6. คลิก "Submit"
7. **ตรวจสอบ:**
   - ไฟล์ upload สำเร็จ
   - Milestone status เปลี่ยนเป็น "Submitted"
   - ไฟล์แสดงในรายการ

### 5. ทดสอบ Final File Submission

1. เปิด Project ที่สร้างไว้
2. ไปที่แท็บ "Final Submissions"
3. คลิก "Upload Final File" หรือ "Submit Final"
4. เลือกไฟล์ที่ต้องการ upload
5. กรอกข้อมูลเพิ่มเติม (ถ้ามี)
6. คลิก "Submit"
7. **ตรวจสอบ:**
   - ไฟล์ upload สำเร็จ
   - Final submission แสดงในรายการ
   - Status เปลี่ยนเป็น "Submitted"

## 🔍 การตรวจสอบ Errors

### Backend Errors
- ดูที่ terminal ที่รัน `python manage.py runserver`
- ตรวจสอบ Django error messages
- ตรวจสอบ database connection

### Frontend Errors
- เปิด Browser DevTools (F12)
- ดูที่ Console tab
- ดูที่ Network tab สำหรับ API errors

### Common Issues

#### 1. Backend 500 Error
**สาเหตุ:** อาจมีปัญหาใน serializer หรือ database
**วิธีแก้:**
- ตรวจสอบ Django logs
- ตรวจสอบว่า database มีข้อมูลครบหรือไม่
- ตรวจสอบ permissions

#### 2. Frontend API Error
**สาเหตุ:** Backend ไม่ทำงานหรือ URL ไม่ถูกต้อง
**วิธีแก้:**
- ตรวจสอบว่า backend server ทำงานอยู่
- ตรวจสอบ API URL ใน frontend code
- ตรวจสอบ CORS settings

#### 3. CORS Error
**สาเหตุ:** Backend ไม่ได้ allow frontend origin
**วิธีแก้:**
- ตรวจสอบ `CORS_ALLOWED_ORIGINS` ใน `settings.py`
- เพิ่ม `http://localhost:5173` ใน allowed origins

## 📊 Checklist การทดสอบ

### Register Project
- [ ] Modal เปิดได้
- [ ] กรอกข้อมูลได้
- [ ] เลือก Advisor ได้
- [ ] เพิ่ม Students ได้
- [ ] Submit สำเร็จ
- [ ] ไม่มี errors ใน console
- [ ] Project ปรากฏในรายการ

### Milestone Submission
- [ ] เปิดหน้า Milestones ได้
- [ ] Upload ไฟล์ได้
- [ ] Submit สำเร็จ
- [ ] Status เปลี่ยนถูกต้อง
- [ ] ไฟล์แสดงในรายการ

### Final File Submission
- [ ] เปิดหน้า Final Submissions ได้
- [ ] Upload ไฟล์ได้
- [ ] Submit สำเร็จ
- [ ] Status เปลี่ยนถูกต้อง
- [ ] ไฟล์แสดงในรายการ

## 🎯 สรุป

หลังจากแก้ไข Backend และ Frontend แล้ว ควรจะ:
1. ✅ Backend API ทำงานได้ (ไม่ใช่ 500 error)
2. ✅ Frontend ไม่มี runtime errors
3. ✅ Register Project ทำงานได้
4. ✅ Milestone Submission ทำงานได้
5. ✅ Final File Submission ทำงานได้

หากยังมีปัญหา ให้ตรวจสอบ:
- Backend logs
- Frontend console errors
- Network requests/responses
- Database data

